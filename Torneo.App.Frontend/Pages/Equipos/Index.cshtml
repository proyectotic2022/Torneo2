@page
@model Torneo.App.Frontend.Pages.Equipos.IndexModel
@{
}
<h1>Listado de Equipos</h1>
<a class="btn btn-primary" style="background:blue" asp-page="./Create">Nuevo Equipo</a>
<form method="get">
  <div class="form-group">
    <label for="b">Buscar</label>
    <input name="b" id="b" class="form-control" asp-for="@Model.bActual">
  </div>
  <button type="submit" class="btn btn-primary">Buscar</button>
</form>
<table class="table">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Nombre</th>
      <th scope="col">Acciones</th>
    </tr>
  </thead>
  <tbody>
      @foreach(var equipo in Model.equipos)
      {
        <tr>
            <td>@equipo.nombre</td>
            <td>
                <a class="btn btn-primary" asp-page="./Details" asp-route-id="@equipo.id">Detalle</a>
                <a class="btn btn-secondary" style="background:green" asp-page="./Edit" asp-route-id="@equipo.id">Editar</a>
                <a class="btn btn-secondary" style="background:red" asp-page="./Delete" asp-route-id="@equipo.id">Eliminar</a>                
            </td>
        </tr>
      }
  </tbody>
</table> 