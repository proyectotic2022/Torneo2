@page
@model Torneo.App.Frontend.Pages.Novedades.IndexModel
@{
}
<h1>Listado de Novedades</h1>
<a class="btn btn-primary" style="background:blue" asp-page="./Create">Nueva Novedad</a>
<form method="get">
  <div class="form-group">
    <label for="b">Buscar</label>
    <input name="b" id="b" class="form-control" asp-for="@Model.bActual">
  </div>
  <button type="submit" class="btn btn-primary">Buscar</button>
</form>
<table class="table">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Nombre</th>
      <th scope="col">Minuto</th>
      <th scope="col">Acciones</th>      
    </tr>
  </thead>
  <tbody>
      @foreach(var novedad in Model.novedades)
      {
        <tr>
            <td>@novedad.nombre</td>
            <td>@novedad.minuto</td>
            <td>
                <a class="btn btn-primary" asp-page="./Details" asp-route-id="@novedad.id">Detalle</a>
                <a class="btn btn-secondary" style="background:red" asp-page="./Edit" asp-route-id="@novedad.id">Editar</a>
                <a class="btn btn-secondary" style="background:green" asp-page="./Delete" onclick="return confirm('Está seguro que desea eliminar el registro?')" asp-route-id="@novedad.id">Eliminar</a>                
            </td>
        </tr>
      }
  </tbody>
</table> 